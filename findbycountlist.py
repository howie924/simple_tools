#find element which counts = k in a give list
def(list, k):

    oneindexlist = []
    s = set(List)
    for i in s:
        if List.count(i) == k:
            oneindexlist.append(List.index(i))
    return oneindexlist